# How to Use & Integrate This Project

## Prerequisites & Setup
Ensure you have the spatial data engine libraries installed in your R environment:
```r
install.packages(c("sf", "dplyr", "ggplot2"))

# Load packages
library(sf)
library(dplyr)
library(ggplot2)

# STEP 1: READ STATE SHAPEFILE
states <- st_read("NGA_adm1.shp")

# STEP 2: REMOVE NON-STATE FEATURE
states <- states %>%
  filter(NAME_1 != "Water body")

# STEP 3: CREATE GEOPOLITICAL ZONE LOOKUP TABLE
zone_lookup <- data.frame(
  NAME_1 = c(
    
    # South East
    "Abia",
    "Anambra",
    "Ebonyi",
    "Enugu",
    "Imo",
    
    # South South
    "Akwa Ibom",
    "Bayelsa",
    "Cross River",
    "Delta",
    "Edo",
    "Rivers",
    
    # South West
    "Ekiti",
    "Lagos",
    "Ogun",
    "Ondo",
    "Osun",
    "Oyo",
    
    # North Central
    "Benue",
    "Kogi",
    "Kwara",
    "Nassarawa",
    "Niger",
    "Plateau",
    "Federal Capital Territory",
    
    # North East
    "Adamawa",
    "Bauchi",
    "Borno",
    "Gombe",
    "Taraba",
    "Yobe",
    
    # North West
    "Jigawa",
    "Kaduna",
    "Kano",
    "Katsina",
    "Kebbi",
    "Sokoto",
    "Zamfara"
  ),
  
  zone = c(
    rep("South East", 5),
    rep("South South", 6),
    rep("South West", 6),
    rep("North Central", 7),
    rep("North East", 6),
    rep("North West", 7)
  )
)

# STEP 4: JOIN STATES TO GEOPOLITICAL ZONES
states_zone <- states %>%
  left_join(zone_lookup, by = "NAME_1")
  
# STEP 5: CHECK FOR UNMATCHED STATES
states_zone %>%
  filter(is.na(zone)) %>%
  select(NAME_1)

# STEP 6: DISSOLVE STATES INTO GEOPOLITICAL ZONES
zones <- states_zone %>%
  group_by(zone) %>%
  summarise()

# STEP 8: GENERATE GEOPOLITICAL MAP
zone_map <- ggplot(zones) +
  
  geom_sf(
    aes(fill = zone),
    color = "black",
    linewidth = 0.5
  ) +
  
  geom_sf_text(
    aes(label = zone),
    size = 3.5,
    fontface = "bold"
  ) +
  
  labs(
    title = "Nigeria Geopolitical Zones",
    subtitle = "Derived from State Administrative Boundaries",
    fill = "Zone"
  ) +
  
  theme_minimal() +
  
  theme(
    plot.title = element_text(
      hjust = 0.5,
      face = "bold",
      size = 16
    ),
    
    plot.subtitle = element_text(
      hjust = 0.5,
      size = 12
    ),
    
    legend.position = "right",
    
    axis.text = element_blank(),
    axis.ticks = element_blank()
  )

# Display map
zone_map

## Licensing & Terms of Use

This project is open-source and dual-licensed to account for both the code pipeline and the underlying geographical data dependencies:

### 1. Code License (MIT License)
The R source code (`.R` scripts), mapping logic, data cleaning algorithms, and custom `ggplot2` configurations are licensed under the **MIT License**. 

This means you are free to:
* **Commercial Use:** Use this code in commercial applications or reporting.
* **Modification:** Change, refactor, or build upon the R functions.
* **Distribution:** Share the code or include it in your own packages.

*Condition:* You must include the original copyright notice and permission notice in any substantial portions of the software.

### 2. Data Dependency & Attribution Notice
The administrative boundaries (`NGA_adm1.shp`) used to generate these zones are derived from public geographic datasets (such as GADM or GRID3). 
* While this project provides the logical mapping matrix to dissolve them into zones, users should respect the original data source's license if republishing the raw spatial files.
* Map outputs generated by this script are free to use in academic, corporate, or personal presentations with a voluntary attribution link back to this repository.
